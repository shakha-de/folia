meta {
  name: Create a tree corrupted with no lat
  type: http
  seq: 3
}

post {
  url: {{host}}/api/trees
  body: json
  auth: bearer
}

auth:bearer {
  token: {{user1Token}}
}

body:json {
  {
    "species": "Quercus robur",
    "commonName": "Stieleiche",
    "lng": 11.9693,
    "soilMoistureLevel": "MODERATE",
    "healthStatus": "HEALTHY",
    "metadata": {
      "plantedDate": "2023-05-15",
      "caretakerId": "staff_042"
    }
  }
}

assert {
  res.status: eq 400
  res.body.success: eq false
  res.body.message: contains Validation failed.
  res.body.errors: isNotEmpty
  res.body.errors.lat: contains Latitude is required
}

settings {
  encodeUrl: true
  timeout: 0
}
