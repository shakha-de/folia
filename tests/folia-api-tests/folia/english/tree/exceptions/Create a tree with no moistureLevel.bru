meta {
  name: Create a tree with no moistureLevel
  type: http
  seq: 5
}

post {
  url: {{host}}/api/trees
  body: json
  auth: bearer
}

auth:bearer {
  token: {{user1Token}}
}

body:json {
  {
    "species": "Quercus robur",
    "commonName": "Stieleiche",
    "lat": 51.4822,
    "lng": 11.9693,
    "healthStatus": "HEALTHY",
    "metadata": {
      "plantedDate": "2023-05-15",
      "caretakerId": "staff_042"
    }
  }
}

assert {
  res.status: eq 400
  res.body.success: eq false
  res.body.errors: isNotEmpty
  res.body.message: contains Validation failed.
  res.body.errors.soilMoistureLevel: contains Soil moisture is required
}

settings {
  encodeUrl: true
  timeout: 0
}
