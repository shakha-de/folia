# Testcontainers Configuration
# Disable checks that might fail in CI
checks.disable=true

# Ryuk container cleanup
ryuk.disabled=false

# Reuse containers between tests
testcontainers.reuse.enable=false

# Logging
testcontainers.logging=INFO

